{
  "name": "markdown-it-markmap",
  "version": "0.1.0",
  "description": "Markmap plugin for markdown-it markdown parser.",
  "keywords": [
    "markdown-it-plugin",
    "markdown-it",
    "markdown",
    "markmap",
    "mindmap"
  ],
  "repository": "https://github.com/deiv/markdown-it-markmap.git",
  "license": "MIT",
  "scripts": {
    "build": "rimraf build && rollup -c",
    "test": "rimraf build && rollup -c && node test/test.js"
  },
  "files": [
    "src/index.js"
  ],
  "devDependencies": {
    "assert": "^2.0.0",
    "markdown-it": "^8.4.2",
    "markdown-it-testgen": "~0.1.0",
    "rimraf": "^3.0.2",
    "rollup": "^2.8.2"
  },
  "dependencies": {
    "markmap-lib": "^0.7.4"
  }
}
